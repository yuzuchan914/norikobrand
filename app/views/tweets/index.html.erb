<!-- 上段ヘッダー：ロゴだけ -->
<header class="header-logo">
  <div class="logo-wrapper">
    <%= image_tag "norikobrand_logo_white.png", alt: "norikobrandロゴ", width: 45, height: 45 %>
  </div>
</header>

<div class="gototop2">
<%= link_to "トップに戻る", tweets_top_path %>
<div>

<section class="products_title2">
  <div class="products-header2">
    <h2>商品一覧</h2>
    <h6>products</h6>
  </div>

  <% if user_signed_in? && current_user.email == "takami_japan08@live.jp" %>
  <div class="link">
    <%= link_to "新規投稿へ", new_tweet_path, class: "btn btn-primary", style: "margin-bottom: 20px; display: inline-block;" %>
  </div>
  <% end %>

 <div class="shouhinnnosetumei2">
  <p>「体に 優しい 美味しい 楽しい」考えられるすべてのホスピタリティーの向上を意識しています。</p>
  <p>受注発注ですので、発送まで数日～数週間かかる場合がございます。ご了承ください。</p>
 </div>
</section>

<section class="products-section2">
  <% if @products.any? %>
    <ul class="product-list2">
      <% @products.each do |product| %>
        <li class="product-card2 anim-box popup is-animated">
          <% if product.image.attached? %>
            <%= image_tag product.image, class: "product-image2" %>
          <% end %>

          <h3 class="product-name2"><%= product.name %></h3>

          <% if user_signed_in? && (current_user.email == "takami_japan08@live.jp" || current_user.email == "masayan090909@@") %>
            <%= link_to "編集", edit_tweet_path(product) %>
          <% end %>

          <p class="product-desc2">¥<%= product.price %></p>
          <p class="product-price2">
            <%= product.description.to_s.split(/。/)[0..1].join("。") + "。" %>
          </p>

          <div class="button-row">
            <%= link_to "詳細", tweet_path(product.id), class: "unified-button2" %> 
            <%= link_to "購入", new_tweet_order_path(product), class: "unified-button2" %>

            <% if user_signed_in? %>
              <%= link_to "#", class: "unified-button2", disabled: true do %>
                <%= image_tag "kart.png", alt: "買い物かご", width: 24, height: 24 %>
              <% end %>
            <% end %>
          </div> <!-- ←ここで正しくbutton-rowを閉じる -->

        </li> <!-- ←ループ内でちゃんと閉じる -->
      <% end %>
    </ul>

    <!-- ★ ふわふわたち：sectionの中・ulの外に正しく配置 -->
    <div class="floating-wrapper">
      <div class="fuwafuwa delay1" style="background-image: url(<%= asset_path('太陽1.webp') %>); top: 5%; left: 10%;"></div>
      <div class="fuwafuwa delay2" style="background-image: url(<%= asset_path('太陽2.png') %>); top: 15%; right: 5%;"></div>
      <div class="fuwafuwa delay3" style="background-image: url(<%= asset_path('太陽3.png') %>); bottom: 20%; left: 15%;"></div>
      <div class="fuwafuwa delay4" style="background-image: url(<%= asset_path('太陽4.png') %>); bottom: 10%; right: 10%;"></div>
    </div>
  
  <% else %>
    <p>商品はまだ登録されていません。</p>
  <% end %>
</section>

<footer class="footer-brand">
  <p>@NorikoBrand</p>
</footer>